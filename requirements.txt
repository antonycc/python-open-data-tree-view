# Libraries for Tree View
# Updating: `pip freeze` and manually set the packages that are required 
# Testing: `virtualenv --clear venv` then install and run unit tests
Flask>=1.0.2
logmatic-python>=0.1.7
pandas>=0.23.4
werkzeug>=0.14.1